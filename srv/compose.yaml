name: acob-srv

services:
  acob-srv:
    image: acob-srv:latest
    container_name: acob-srv
    build: .
    environment:
      ACOB_SRV_HOST: ${ACOB_SRV_HOST:-0.0.0.0}
      ACOB_SRV_PORT: ${ACOB_SRV_PORT:-58347}
    ports:
      - "${ACOB_SRV_PORT:-58347}:${ACOB_SRV_PORT:-58347}"
